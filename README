Website is launched on PORT 3000: http://localhost:3000/

To start server open a terminal and run 'npm start'

starting the server will open a preferred browser automatically and start the process.

This portfolio is built with HTML, CSS, NODEMON, and NODE.JS. View package.json file to see all dependencies and scripts.

If you are catching errors for the PORT 3000, open a terminal on your machine and run 'lsof -ls' to view what process if being run on PORT 3000. After you have done this kill the process using 'kill -9 PID' command line. Then proceed with running 'npm start'.

This is a project by Ryan Harricharan, All Rights Reserved. This is not an open source application. Last updated 2024.